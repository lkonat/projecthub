.claude

# Ignore node_modules in any directory, at any depth

**/node_modules/

# Ignore all .db files

**/*.db

# Ignore env files (may contain secrets like GITHUB_TOKEN) at any depth.
# .env.example is a different filename and stays tracked.

**/.env